# This file was auto-generated by the Firebase CLI
# Updated by ChatGPT to correctly build & deploy React app

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      # Clone GitHub repo
      - uses: actions/checkout@v4

      # Install dependencies
      - name: Install dependencies
        run: npm install

      # Build React project (generates /build folder)
      - name: Build React App
        run: npm run build

      # Deploy to Firebase Hosting
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_CREATIVE_STICHING_BOUTIQUE }}
          channelId: live
          projectId: creative-stiching-boutique
